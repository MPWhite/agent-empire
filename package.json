{
  "name": "risk-mmo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "engine",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run build:engine && concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build:engine": "npm run build -w engine"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
